/** @type {import('next').NextConfig} */
const nextConfig = {
  output: 'export',
  basePath: '/employee_management_dashboard',
  images: {
    unoptimized: true,
  },
};

module.exports = nextConfig;

